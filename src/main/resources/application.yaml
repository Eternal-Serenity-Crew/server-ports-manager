server:
  port: ${SERVER_INTERNAL_PORT}

spring:
  application:
    name: server-ports-manager
  aop:
    auto: true
    proxy-target-class: true

database:
  url: ${DB_URL}
  username: ${DB_USER}
  password: ${DB_PASSWORD}
  schema: public